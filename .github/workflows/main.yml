name: 🚀 Gemini Persistent Profile API Gateway (Serveo Ultimate)

on:
  workflow_dispatch:

jobs:
  run-api-gateway:
    name: 🌐 Launch Persistent API + Serveo Fixed Tunnel
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout Repository Code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js Environment
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📥 Install System & Node Dependencies
        run: |
          npm init -y
          npm install express playwright playwright-extra puppeteer-extra-plugin-stealth multer
          npx playwright install chrome --with-deps

      - name: 🖥️ Start Virtual Display (Xvfb)
        run: |
          sudo apt-get install -y xvfb
          Xvfb :99 -screen 0 1920x1080x24 &
          sleep 2
          echo "DISPLAY=:99" >> $GITHUB_ENV

      - name: 📂 Download & Extract Google Profile Securely
        env:
          DOWNLOAD_URL: ${{ secrets.PROFILE_DOWNLOAD_URL }}
        run: |
          if [ -z "$DOWNLOAD_URL" ]; then
            echo "❌ Error: PROFILE_DOWNLOAD_URL secret is missing!"
            exit 1
          fi
          wget -q -O gemini_profile.zip "$DOWNLOAD_URL"
          unzip -q gemini_profile.zip -d ./
          rm -f ./gemini_profile/SingletonLock
          rm -f ./gemini_profile/Lock
          rm -f ./gemini_profile/Google\ Profile\ Lock
          rm -rf ./gemini_profile/Default/Cache/*
          rm -f gemini_profile.zip
          echo "✅ Google Profile Ready!"

      - name: 🛠️ Create & Start API Gateway Server
        run: |
          mkdir -p /tmp/gemini-recordings

          cat > server.mjs << 'SERVER_EOF'
          import express from 'express';
          import { chromium } from 'playwright-extra';
          import StealthPlugin from 'puppeteer-extra-plugin-stealth';
          import fs from 'fs';
          import os from 'os';
          import path from 'path';

          chromium.use(StealthPlugin());
          const app = express();
          
          // Base64 ইমেজ হ্যান্ডেল করার জন্য মেমোরি লিমিট ৫০ মেগাবাইট করা হলো
          app.use(express.json({ limit: '50mb' }));
          app.use(express.urlencoded({ limit: '50mb', extended: true }));

          const PORT = 8080;
          let context, page;

          async function initGemini() {
            try {
              console.log("⚡ Launching Local Google Chrome Instance...");
              context = await chromium.launchPersistentContext('./gemini_profile', {
                headless: false,
                channel: 'chrome',
                args: [
                  '--no-sandbox', 
                  '--disable-setuid-sandbox',
                  '--disable-blink-features=AutomationControlled',
                  '--disable-web-security',
                  '--disable-gpu'
                ],
                viewport: { width: 1280, height: 800 },
                recordVideo: { dir: '/tmp/gemini-recordings/', size: { width: 1280, height: 800 } }
              });
              page = await context.newPage();
              await page.goto('https://gemini.google.com/app', { waitUntil: 'domcontentloaded' });
              console.log("✅ Gemini Browser Ready!");
            } catch (err) {
              console.error("❌ Init failed:", err.message);
            }
          }

          app.post('/api/gemini', async (req, res) => {
            if (!page) return res.status(500).json({ success: false, error: 'Browser stack not ready' });
            
            const promptText = req.body.prompt;
            const base64Image = req.body.image; 
            
            if (!promptText) return res.status(400).json({ error: 'Prompt is required' });

            let tempFilePath = null;

            try {
              console.log(`🤖 [API] Request received. Base64 Image Present: ${!!base64Image}`);
              
              // New Chat SPA Button Click (Fix 408)
              const newChatBtnSel = 'a[href="/app/new"], button:has-text("New chat"), [aria-label="New chat"]';
              const newChatBtn = await page.$(newChatBtnSel);
              if (newChatBtn) {
                await newChatBtn.click();
                await new Promise(r => setTimeout(r, 1200)); 
              } else {
                await page.goto('https://gemini.google.com/app', { waitUntil: 'commit' }); 
                await new Promise(r => setTimeout(r, 2000));
              }
              try { await page.keyboard.press('Escape'); } catch(_) {}

              // Base64 ইমেজ আপলোড লজিক
              if (base64Image) {
                tempFilePath = path.join(os.tmpdir(), `upload_${Date.now()}.jpg`);
                fs.writeFileSync(tempFilePath, Buffer.from(base64Image, 'base64'));

                const uploadBtnSelectors = [
                  'button[aria-label="Open upload file menu"]',
                  'button[aria-label="Upload file"]',
                  'button[aria-label="Add image"]',
                  'button[aria-label="Attach files"]',
                ];

                let uploadBtnClicked = false;
                for (const sel of uploadBtnSelectors) {
                  try {
                    const btn = await page.$(sel);
                    if (btn) { await btn.click(); uploadBtnClicked = true; break; }
                  } catch (_) {}
                }

                if (uploadBtnClicked) {
                  const fileInput = await page.waitForSelector('input[type="file"]', { timeout: 5000 });
                  if (fileInput) {
                    await fileInput.setInputFiles(tempFilePath);
                    await new Promise(r => setTimeout(r, 1500));
                  }
                }
              }

              // প্রম্পট পাঠানো
              const inputSel = 'div.ql-editor[contenteditable="true"], div[role="textbox"], textarea';
              await page.waitForSelector(inputSel, { timeout: 5000 });
              await page.click(inputSel, { force: true });
              await page.keyboard.press('Control+a');
              await page.keyboard.press('Delete');
              await page.keyboard.insertText(promptText);
              await page.keyboard.press('Enter');

              const responseSel = 'div[id^="model-response-message-content"], message-content, .message-content-inner';
              await page.waitForSelector(responseSel, { timeout: 15000 });

              res.setHeader('Content-Type', 'text/event-stream');
              res.setHeader('Cache-Control', 'no-cache');
              res.setHeader('Connection', 'keep-alive');

              let lastSentText = "";
              let noChangeCounter = 0;

              while (true) {
                await new Promise(r => setTimeout(r, 200));
                const currentFullText = await page.evaluate((selector) => {
                  const el = document.querySelector(selector);
                  return el ? el.innerText.trim() : '';
                }, responseSel);

                if (currentFullText.length > lastSentText.length) {
                  const newChunk = currentFullText.substring(lastSentText.length);
                  res.write(newChunk);
                  lastSentText = currentFullText;
                  noChangeCounter = 0;
                } else if (currentFullText.length > 0 && currentFullText.length === lastSentText.length) {
                  noChangeCounter++;
                  const isFinished = await page.evaluate((selector) => {
                    const el = document.querySelector(selector);
                    return el ? (!el.innerHTML.includes('loading') && !el.innerHTML.includes('...')) : true;
                  }, responseSel);
                  if (noChangeCounter > 5 && isFinished) break;
                }
              }
              res.end();
            } catch (err) {
              console.error(`❌ [API ERROR]`, err.message);
              if (!res.headersSent) res.status(500).json({ success: false, error: err.message });
              else res.end();
            } finally {
              if (tempFilePath && fs.existsSync(tempFilePath)) fs.unlinkSync(tempFilePath);
            }
          });

          app.listen(PORT, '0.0.0.0', async () => {
            console.log(`🚀 API Server active on port ${PORT}`);
            await initGemini();
          });
          SERVER_EOF

          node server.mjs &
          sleep 5

      - name: 🔗 Bind & Expose via Serveo (No Timeout & Fixed Subdomain)
        run: |
          echo "🚀 Starting Permanent Fixed Serveo SSH Tunnel..."
          # 'ruslan-gemini' সাবডোমেনটি আপনার জন্য আজীবনের জন্য লক হয়ে থাকবে
          ssh -R ruslan-gemini:80:localhost:8080 serveo.net -o StrictHostKeyChecking=no -o ServerAliveInterval=30 -o ServerAliveCountMax=5 > /tmp/serveo.log 2>&1 &
          
          sleep 8
          echo "--------------------------------------------------------"
          echo "🔗 YOUR PERMANENT FIXED API GATEWAY ENDPOINT:"
          echo "https://ruslan-gemini.serveo.net/api/gemini"
          echo "--------------------------------------------------------"
          cat /tmp/serveo.log
          
          sleep 18000

      - name: 🎥 Upload Live Screen Recording & Debug Assets
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: gemini-live-recording
          path: /tmp/gemini-recordings/
          retention-days: 3
